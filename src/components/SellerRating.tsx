import { Star } from "lucide-react";

// Temporarily disabled due to missing database functions
const SellerRating = ({ sellerId, size = "sm", className = "" }: { sellerId: string | null; size?: "sm" | "md" | "lg"; className?: string }) => {
  // Return empty component temporarily
  return null;
};

export default SellerRating;
